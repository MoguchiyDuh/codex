[project]
name = "codex"
version = "0.1.0"
description = "Learning repository for algorithms, data structures, and math"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.9",
    "numpy",
]

[dependency-groups]
dev = []
