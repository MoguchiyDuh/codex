# C Learning

Theory notes grounded in `~/Documents/c_learning/` source files.

## Topics

- [[Memory & Pointers]] — variables as memory, `&`/`*`, pointer arithmetic, `sizeof`
- [[Casting & Type Aliasing]] — C casts, strict aliasing, `char*`, `memcpy`
- [[IEEE 754]] — float layout, raw bits, infinity, NaN
- [[Stack vs Heap]] — lifetimes, when to use each, dangling pointers, overflow, ASan
- [[Strings]] — null-terminated arrays, `my_strlen`/`my_strcpy`/`my_strcmp`/`my_strchr`
- [[Heap Memory]] — `malloc`/`free`/`calloc`/`realloc`, corruption types, dynamic array
- [[HashMap]] — hash function, separate chaining, insert/get/delete, memory ownership
