# Index

> Matplotlib + Seaborn notes. Structured after university data visualization curriculum.

## Notes

- [[Plot Cheatsheet]] — `plt.plot()` / `ax.plot()` args, pyplot vs OO style, markers, linestyles, grid, legend.
- [[Matplotlib vs Seaborn]] — when to use each, function-level comparison, `hist()` vs `histplot()`, customization flow.
- [[Histplot Cheatsheet]] — `sns.histplot()` all args, `stat=`/`multiple=`/`element=` tables, KDE overlay, axes-level vs figure-level.
- [[Axes vs Figure Level]] — axes-level vs figure-level API split, FacetGrid usage, decision rule table.
- [[Kdeplot Cheatsheet]] — `sns.kdeplot()` all args, `bw_adjust`, `fill=`, `cumulative=`, bivariate KDE.
- [[Scatterplot Cheatsheet]] — `sns.scatterplot()` all args, `hue=`/`size=`/`style=` behavior.
- [[Regplot Cheatsheet]] — `sns.regplot()` all args, `ci=`, `order=`, `logistic=`, `lowess=`, no `hue=` caveat.
- [[Lineplot and Melt Cheatsheet]] — `sns.lineplot()` args, wide vs long format, `df.melt()` explained.
- [[Fill Between Cheatsheet]] — `ax.fill_between()` args, threshold highlight, two-curve fill, confidence band.
- [[Pie Chart Cheatsheet]] — `ax.pie()` args, `explode=`, `autopct=`, donut with `wedgeprops`.
- [[Pairplot Cheatsheet]] — `sns.pairplot()` args, `kind=`/`diag_kind=` tables, `PairGrid` access, `corner=`.
- [[Heatmap Cheatsheet]] — `sns.heatmap()` all args, colormap table, correlation matrix pattern, triangle mask.
- [[Box and Violin Cheatsheet]] — `sns.boxplot()` / `sns.violinplot()` all args, `inner=` table, `split=`, `sharey=`.
- [[Barplot Cheatsheet]] — `sns.barplot()` all args, `estimator=` table, `errorbar=` table, vs `ax.bar()`.
- [[Swarmplot Cheatsheet]] — `sns.swarmplot()` all args, `dodge=`, overlay pattern on boxplot.
- [[Subplots Cheatsheet]] — `plt.subplots()` all args, axes access patterns, `sharex=`/`sharey=`, `squeeze=`, seaborn injection.

## See also

- [[../../python/oop/notes/Index|OOP Notes]]
